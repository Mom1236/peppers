export function AnnouncementBar() {
  return (
    <div className="sticky top-0 z-50 border-b border-slate-200 bg-white/70 backdrop-blur">
      <div className="mx-auto max-w-7xl px-4 py-2 text-center text-xs font-semibold text-slate-700">
        RESEARCH USE ONLY — SAME DAY SHIPPING ON ORDERS BEFORE 1PM
      </div>
    </div>
  );
}
